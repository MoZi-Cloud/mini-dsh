# mini-DSH real-use log (§33)

English | [中文](real-use-log.zh.md)

The v1.6b/c/d entry gate (fork-mini-DSH改造方案-v1.6a.md §33) requires continuous real mini-DSH use over at least N work items, no BOOT/4K regression, and owner confirmation of value, with the basis coming from real usage recorded in the ledger. This log records that usage: each entry names the driver, the work, the verifier evidence, and where the durable ledger record lives. N is the owner's decision, not this log's.

## Entries

### 2026-09-18 — the pinned 4K slice (baseline)

One fresh scripted agent completed one ledger task under the pinned 4096-token window with the plan document never entering model context; recorded in the BOOT acceptance report (v1.6a/ section) and reproducible via `./benchmarks/context-light/run-4k.sh`. The golden plan's own execution history — the 15 v1.6a work items driven through the ledger seams — predates it.

### 2026-09-18 — PW-PRESENTERS-001 through the shipped tools

The post-v1.6a real-use lane (`./benchmarks/real-use/run-real-use.sh`) drove `PW-PRESENTERS-001` (Host presenters for the project_work tools, plan `v1.6a/fork-mini-DSH-post-v1.6a.plan.yaml`) through the SHIPPED `MiniProjectLedger` + `mini-project-work` plugins against the persistent profile ledger `~/.dsh/project-ledger/ledger.sqlite`: claim by `agent:mini-real-use-lane`, verifier `pnpm exec vitest run mini-profile` executed as a real subprocess (exit 0), report PASS, item DONE, doctor 0 issues, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v1","workItemId":"wi:mini-dsh:PW-PRESENTERS-001","stableKey":"PW-PRESENTERS-001","verifierCommand":"pnpm exec vitest run mini-profile","verifierExitCode":0,"itemStatus":"DONE","doctorIssues":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, doctor 0 issues, no writes). The remaining plan items — `PW-REPORT-VERDICTS-001`, `PW-OBSERVED-TAIL-001` — stay claimable in the same ledger for later increments.

### 2026-09-18 — PW-REPORT-VERDICTS-001 through the shipped tools

The same lane drove `PW-REPORT-VERDICTS-001` (per-criterion verdicts in `project_work_update` reports: one `{criterionId, result PASS|FAIL, optional exitCode}` entry per observable acceptance criterion, exactly once each, replacing the single blanket verdict) against the persistent ledger. The report grammar changed under the lane in the same increment: the lane now runs each observable criterion's stored command from the WorkPacket and reports one verdict per criterion, and its JSON line gained per-criterion `verifierResults`. Claim by `agent:mini-real-use-lane`, verifier `pnpm exec vitest run mini-profile` (exit 0), item DONE, doctor 0 issues, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v1","workItemId":"wi:mini-dsh:PW-REPORT-VERDICTS-001","stableKey":"PW-REPORT-VERDICTS-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-REPORT-VERDICTS-001:AC-PW-REPORT-VERDICTS-001","command":"pnpm exec vitest run mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, no writes). The 4K slice rerun after the grammar change stayed green (6 requests, max 2,427/4,096 estimated tokens, DONE), so no BOOT/4K regression accompanies this increment. The remaining plan item — `PW-OBSERVED-TAIL-001` — stays claimable for a later increment.

### 2026-09-18 — PW-OBSERVED-TAIL-001 through the shipped tools

The same lane drove `PW-OBSERVED-TAIL-001` (bounded verifier output tails in `project_work_update` reports: each verdict entry gained an optional `outputTail`, of which the report stores at most the final 2048 characters in the evaluation's observed payload beside the exit code, so a replayed ledger explains a verdict without re-running the command) against the persistent ledger. The lane now passes each executed verifier's captured output as the tail and asserts the bounded tail landed in `acceptance_evaluations.observed_json`. Claim by `agent:mini-real-use-lane`, verifier `pnpm exec vitest run mini-profile` (exit 0), item DONE, doctor 0 issues, event replay DONE; the recorded evaluation's observed payload holds `{exitCode: 0, outputTail: 1785 chars of real vitest output}`. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v1","workItemId":"wi:mini-dsh:PW-OBSERVED-TAIL-001","stableKey":"PW-OBSERVED-TAIL-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-OBSERVED-TAIL-001:AC-PW-OBSERVED-TAIL-001","command":"pnpm exec vitest run mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, no writes). The 4K slice rerun after the change stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE — the scripted report now carries a short output tail), so no BOOT/4K regression accompanies this increment. This closes the post-v1.6a increment backlog; later real-use items come from new plan versions.

### 2026-09-18 — plan version 2 and PW-ITEM-REVIEW-001 through the shipped tools

The plan document (`v1.6a/fork-mini-DSH-post-v1.6a.plan.yaml`) bumped to version 2 with a fresh batch (`PW-ITEM-REVIEW-001`, `PW-REPLAY-VIEW-001`), and the same lane drove the bump end to end against the persistent ledger: version 2 imported, version 1 superseded through the §22 seam (`supersedePlanVersion` naming the successor — the seam's first real-use record, event sequence 29; the three version-1 items stay DONE and byte-identical), then `PW-ITEM-REVIEW-001` (the `/project item` review view over a new `readWorkItemReview` ledger seam) claimed by `agent:mini-real-use-lane`, verifier `pnpm exec vitest run mini-profile` (exit 0), item DONE, doctor 0 issues, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v2","workItemId":"wi:mini-dsh:PW-ITEM-REVIEW-001","stableKey":"PW-ITEM-REVIEW-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-ITEM-REVIEW-001:AC-PW-ITEM-REVIEW-001","command":"pnpm exec vitest run mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, no writes). The 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). `PW-REPLAY-VIEW-001` stays claimable in version 2 for a later increment.

### 2026-09-19 — PW-REPLAY-VIEW-001 through the shipped tools

The same lane drove `PW-REPLAY-VIEW-001` (the `/project replay` audit view over a new `readProjectReplay` ledger seam: the project's events fold, and the rebuilt projection is compared with the materialized rows family by family and in both directions, so a row nothing replays and a replay nothing materializes are both drift) against the persistent ledger. The lane's own passing bar gained the audit in the same increment — `assertReplayClean` runs on both the completion and already-complete paths and the report line carries `replayDrift`. Claim by `agent:mini-real-use-lane`, verifier `pnpm exec vitest run mini-profile` (exit 0), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE; the project timeline now holds 43 events. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v2","workItemId":"wi:mini-dsh:PW-REPLAY-VIEW-001","stableKey":"PW-REPLAY-VIEW-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-REPLAY-VIEW-001:AC-PW-REPLAY-VIEW-001","command":"pnpm exec vitest run mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, `replayDrift: 0`, no writes). The 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). Both version-2 items are now complete; later real-use items come from new plan versions.

### 2026-09-19 — plan version 3 and PW-DOCSYNC-GREEN-001 through the shipped tools

The plan document bumped to version 3 with a fresh batch (`PW-DOCSYNC-GREEN-001`, `PW-EVIDENCE-DIGEST-001`), and the same lane drove the bump end to end against the persistent ledger: version 3 imported, version 2 superseded through the §22 seam (event sequence 47; every prior item stays DONE and byte-identical), then `PW-DOCSYNC-GREEN-001` (the repository's complete doc-sync gate green on the fork: 40 exported-API JSDoc completions, the `ctx.projectLedger` service registered across the cordis catalog, capability-seams tables, and a new bilingual subsystems page, regenerated catalogs with both language sides in sync, and the archived proposals' code fences and package-path references repaired) claimed by `agent:mini-real-use-lane`. Its stored verifier is the gate itself — `pnpm run doc-sync` ran as a real subprocess (exit 0, 41/41 checks, ~6 minutes), which also forced a lane fix: the work plugins now mount with `leaseTtlMs` at twice the verifier timeout, because the default lease expired mid-verifier and the report landed on a lease the reaper owned. Item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v3","workItemId":"wi:mini-dsh:PW-DOCSYNC-GREEN-001","stableKey":"PW-DOCSYNC-GREEN-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-DOCSYNC-GREEN-001:AC-PW-DOCSYNC-GREEN-001","command":"pnpm run doc-sync","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, `replayDrift: 0`, no writes). The 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). `PW-EVIDENCE-DIGEST-001` stays claimable in version 3 for a later increment.

### 2026-09-19 — PW-EVIDENCE-DIGEST-001 through the shipped tools

The same lane drove `PW-EVIDENCE-DIGEST-001` against the persistent ledger: a new `readProjectDigest` ledger read seam aggregates the whole owner record in one read-only pass — every plan with each version's lifecycle status and pinned baseline, every item's criteria tally with its latest verdict counts (the per-criterion latest selection extracted into one helper shared with the review seam, so both keep the same rowid tie-break), and the replay audit verdict embedded as the report itself. `/project digest [<project-id>]` renders it, so the owner reads the whole §33 record without SQL or per-item commands. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run mini-profile` (exit 0), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE; the project timeline now holds 61 events and all seven real-use items are DONE across three versions. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v3","workItemId":"wi:mini-dsh:PW-EVIDENCE-DIGEST-001","stableKey":"PW-EVIDENCE-DIGEST-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-EVIDENCE-DIGEST-001:AC-PW-EVIDENCE-DIGEST-001","command":"pnpm exec vitest run mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, `replayDrift: 0`, no writes). The 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE), and the complete doc-sync gate stayed green (41/41) with the new seam documented bilingually.

### 2026-09-19 — plan version 4 and PW-EXPORT-VIEW-001 through the shipped tools

The plan document bumped to version 4 with a fresh batch (`PW-EXPORT-VIEW-001`, `PW-ITEM-HISTORY-001`), and the same lane drove the bump against the persistent ledger: version 4 imported, version 3 superseded through the §22 seam, then `PW-EXPORT-VIEW-001` — the `/project export` owner evidence export — completed. The digest read seam now carries per-criterion rows (each criterion's status and latest evaluation, the row assembly shared with the review seam through `reviewedCriterionOf`, replacing the tally records), and `/project export [<project-id>]` renders the whole record as one archival markdown block: plan versions with baselines and supersede stamps, per-item completion with per-criterion verdicts, evaluator, time, observed exit code and output-tail excerpt, and the replay audit verdict. Run against this persistent ledger, the export renders 4 plan versions and 9 work items — the whole §33 record without SQL or per-item commands. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run mini-profile` (exit 0), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v4","workItemId":"wi:mini-dsh:PW-EXPORT-VIEW-001","stableKey":"PW-EXPORT-VIEW-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-EXPORT-VIEW-001:AC-PW-EXPORT-VIEW-001","command":"pnpm exec vitest run mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, `replayDrift: 0`, no writes). The 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE), and the complete doc-sync gate stayed green (41/41) with the reshaped digest documented bilingually. `PW-ITEM-HISTORY-001` stays claimable in version 4 for a later increment.

### 2026-09-19 — PW-ITEM-HISTORY-001 through the shipped tools

The same lane drove `PW-ITEM-HISTORY-001` against the persistent ledger: a new `readWorkItemHistory` ledger read seam lists one work item's every recorded evaluation newest-first — verdict, evaluator, time, and observed payload per attempt — the attempts the review's latest-only view collapses away. The item-ref resolution and identity fields moved into shared helpers (`resolveWorkItemRow`, `workItemIdentityOf`) that the review and history seams now both consume, so the two per-item reads keep one resolution contract, one ambiguity error, and one id-branding path. `/project history <stable-key-or-id> [<project-id>]` renders the timeline with the observed exit code and output-tail excerpt per attempt. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run mini-profile` (exit 0), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE; the project timeline now holds 79 events and version 4 is fully complete. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v4","workItemId":"wi:mini-dsh:PW-ITEM-HISTORY-001","stableKey":"PW-ITEM-HISTORY-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-ITEM-HISTORY-001:AC-PW-ITEM-HISTORY-001","command":"pnpm exec vitest run mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, `replayDrift: 0`, no writes). The 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). All nine real-use items across four plan versions are now DONE; later real-use items require a version 5 batch.

### 2026-09-19 — plan version 5 and PW-LEASE-DOCTOR-001 through the shipped tools

The plan document bumped to version 5 with a fresh batch (`PW-LEASE-DOCTOR-001`, `PW-4K-GATE-001`), and the same lane drove the bump against the persistent ledger: version 5 imported, version 4 superseded through the §22 seam (every prior item stays DONE and byte-identical), then `PW-LEASE-DOCTOR-001` — the doctor's lease-staleness check — completed. `planDoctor` now takes the reading clock (`options.nowMs`, defaulting to `Date.now()`) and reports every `work_leases` row that still records ACTIVE past its own expiry, under exactly the reaper's own predicate: the reaper is caller-driven and batched, so a cold or behind reaper leaves those rows, and replay parity cannot see them because the fold projects the same ACTIVE status. The doctor names them instead, so the operator knows recovery is owed; `/project doctor` prints them with no command change, since it renders issues generically. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger mini-profile` (exit 0, both package suites), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE; the project timeline now holds 90 events. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v5","workItemId":"wi:mini-dsh:PW-LEASE-DOCTOR-001","stableKey":"PW-LEASE-DOCTOR-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-LEASE-DOCTOR-001:AC-PW-LEASE-DOCTOR-001","command":"pnpm exec vitest run project-ledger mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, `replayDrift: 0`, no writes). The 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). `PW-4K-GATE-001` — the §33 BOOT/4K no-regression bar as a stored verifier running the pinned 4K lane itself — stays claimable in version 5 for a later increment.

### 2026-09-19 — PW-4K-GATE-001 through the shipped tools

The same lane claimed `PW-4K-GATE-001`, and with it the §33 BOOT/4K no-regression bar entered the ledger record itself: the item's stored verifier runs the pinned 4K context-light real-use slice (`sh benchmarks/context-light/run-4k.sh`) as a real subprocess inside the claim's lease, so the gate's outcome is now ledger data — an acceptance evaluation with the observed exit code, sitting beside this prose log as entry evidence of the kind §33 asks for rather than a design claim. No product code changed hands; the lane's default target moved to this item and the shipped tools did the rest. Claim by `agent:mini-real-use-lane`, verifier exit 0 (the lane observed and reported the 4K lane itself), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE; the project timeline now holds 97 events and version 5 is fully complete. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-post-v16a-increments:v5","workItemId":"wi:mini-dsh:PW-4K-GATE-001","stableKey":"PW-4K-GATE-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PW-4K-GATE-001:AC-PW-4K-GATE-001","command":"sh benchmarks/context-light/run-4k.sh","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

An immediate rerun passed idempotently (`alreadyComplete: true`, no writes). Version 5 is complete in two items; later real-use items require a version 6 batch.

### 2026-09-19 — v1.6b entered: the decision domain lands with its own entry decision

§33(c) was confirmed through the established go-gate after the evidence briefing rendered digest, doctor, and history live from this ledger, and this increment opened the proposal's v1.6b scope by completing `PB-DECISION-RECORD-001` — the first item of a new plan (`mini-dsh-v16b-owner-decision`, its own version chain beside the still-ACTIVE post-v1.6a v5). The decision recording loop: schema version 2 (`decision_requests` / `decision_options` / `decisions` through one appended 1→2 migration), two required vocabulary events (`decision/requested`, `decision/recorded`) bumping the event format to 2 with adjacent reads — a row stamped with an older format decodes, only a newer one rejects, which is what kept this ledger's 97 format-1 events readable across the bump — transactional write seams (`openDecisionRequest`, `recordDecision`), a newest-first `readProjectDecisions`, replay-audit and doctor parity over the new family, and the read-only `/project decisions` view. The lane also caught a real resolution bug on the persistent ledger: a second plan of the same project made implicit `/project` resolution fail ("more than one project: mini-dsh, mini-dsh"); the resolver now deduplicates plan ids into project ids and stays ambiguous only across distinct projects. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 269 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16b-owner-decision:v1","workItemId":"wi:mini-dsh:PB-DECISION-RECORD-001","stableKey":"PB-DECISION-RECORD-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PB-DECISION-RECORD-001:AC-PB-DECISION-RECORD-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

After the run, the owner's §33 entry confirmation itself became the domain's first recorded decision: request `dr:mini-dsh:v1.6b-entry` (options `enter-v1.6b` recommended / `keep-accumulating` / `more-evidence`, raised against the v1.6b plan version) resolved by `owner` selecting `enter-v1.6b` as event 109 — the gate that opened the domain is the domain's first row. The timeline now holds 109 events, 97 stamped format 1 decoding under the format-2 codec beside 12 stamped format 2; the persistent ledger migrated schema 1→2 in place on first open, and the committed v1 fixtures stayed frozen and became the migration's upgrade probes. An immediate rerun passed idempotently (`alreadyComplete: true`, no writes) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). `PB-APPROVAL-RECORD-001` — approvals over typed subject references, kept separate from decisions per the blueprint — stays READY in the v1.6b plan.

### 2026-09-19 — PB-APPROVAL-RECORD-001: approvals land beside their subjects

The approvals loop (blueprint §24) completed as the second item of the v1.6b plan: one `approvals` table over a typed subject reference — `plan-version`, `work-item`, or `decision`, each validated to belong to the same project — with `required_role`/`requested_by`/`decided_by` actor strings and inline decision text, kept separate from decisions per the blueprint's one-line ruling, arriving as schema version 3 through one appended 2→3 migration. Two required vocabulary events (`approval/requested`, `approval/decided`) bump the event format to 3 under the adjacent-reads contract, transactional write seams (`requestApproval`, `decideApproval` — each approval decides exactly once, APPROVED or REJECTED, the schema's CHECKs keeping the deciding columns all-or-nothing with the non-PENDING status), a newest-first `readProjectApprovals` that groups the deciding facts, replay-audit and doctor parity over the new family (subject reference, status, deciding actor, both directions), and the read-only `/project approvals` view. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 287 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16b-owner-decision:v1","workItemId":"wi:mini-dsh:PB-APPROVAL-RECORD-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PB-APPROVAL-RECORD-001:AC-PB-APPROVAL-RECORD-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

After the run, the owner's §33 entry approval became the domain's first rows through the shipped seams: approval `ap:mini-dsh:117` over the v1.6b plan version (`plv:mini-dsh-v16b-owner-decision:v1`, requiring the `owner` role) decided APPROVED as events 117–118 — the timeline's first format-3 rows. The persistent ledger now folds a 1/2/3 mixed timeline clean (97 format-1, 19 format-2, 2 format-3 over 118 events; the seven format-2 rows this run added were written by the previously built surface before the rebuild, their stamp truthful about their writer) and migrated schema 2→3 in place on first open. An immediate rerun on the current build passed idempotently (`alreadyComplete: true`, zero tool calls, doctor 0, drift 0) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). The v1.6b plan is complete in its first two items; actors/roles and the resource domain remain for later increments.


### 2026-09-19 — PB-RESOURCE-RECORD-001: the resource loop lands as plan version 2

The v1.6b plan superseded its own version 1 and this increment completed the version-2 batch's first item: the resource loop (blueprint §25/§26/§27, adapted like the decision and approval domains — project-scoped key uniqueness, inline constraints and spec text, actor strings), arriving as schema version 4 through one appended 3→4 migration. Three required vocabulary events (`resource/required`, `resource/provided`, `resource/verified`) bump the event format to 4 under the adjacent-reads contract; the write seams (`openResourceRequirement`, `provideResourceInstance`, `verifyResourceInstance`) validate JSON-object constraints/metadata/observed text at the parser seam, keep instances serving OPEN requirements only, and store every verifier spec as data — never executed, with re-verification legal per instance. `readProjectResources` nests instances and their verifications under each requirement newest-first, replay-audit and doctor parity cover all three families (identity facts, references, statuses, results — both directions), and the read-only `/project resources` view renders them. The verifier-kind vocabulary is owned by the event codec as `ResourceVerifierKind` (the same five kinds as acceptance, renamed for the resource domain after the codis-catalog name-collision lesson). The lane run also exercised the §22 supersede for real: version 1 of `mini-dsh-v16b-owner-decision` (its two items DONE) froze under version 2, whose fresh stable keys never re-list version 1's. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 302 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16b-owner-decision:v2","workItemId":"wi:mini-dsh:PB-RESOURCE-RECORD-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PB-RESOURCE-RECORD-001:AC-PB-RESOURCE-RECORD-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

After the run, the domain's first rows recorded a real resource of this project: requirement `rr:mini-dsh:persistent-ledger` (ENVIRONMENT, constraints pinning the ledger path and WAL journal, raised against the v1.6b plan version) with instance `ri:mini-dsh:131` — the ledger file itself, provided by the lane — verified PASS against the replay audit that had just folded the whole timeline clean. The persistent ledger now folds a 1/2/3/4 mixed timeline (97 + 19 + 2 + 14 stamps over 132 events) and migrated schema 3→4 in place on first open. An idempotent rerun on the rebuilt surface passed (`alreadyComplete: true`, zero tool calls, doctor 0, drift 0) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). `PB-ACTOR-ROLE-001` — actors and roles as first-class rows — stays READY in version 2.

### 2026-09-19 — PB-ACTOR-ROLE-001: actors and roles become first-class rows

The last family of the proposal's v1.6b scope landed as the version-2 batch's final item: the actor/role domain (blueprint §3/§4, adapted like the decision, approval, and resource domains — the actor strings the other domains already record become the project-scoped `actor_key`, kinds use the ledger's uppercase controlled sets, and the blueprint's unvalued `role_kind` closes to `GOVERNANCE`/`EXECUTION`, the two duties the ledger's roles actually carry), arriving as schema version 5 through one appended 4→5 migration. Three required vocabulary events (`actor/registered`, `role/defined`, `role/assigned`) bump the event format to 5 under the adjacent-reads contract; the write seams (`registerActor`, `defineRole`, `assignRole`) give every `requested_by`/`decided_by`/`raised_by` string and every approval `required_role` reference a durable referent, resolve roles to the actor's project, and enforce one live assignment per actor-role pair (the partial unique index and the seam both refuse a second). `readProjectActors` reads the whole directory with assignment labels resolved through joins, replay-audit and doctor parity cover all three families (identity facts, statuses, pairs, validity windows — both directions), and the read-only `/project actors` view renders them. `INACTIVE` actors and ended assignments are reserved with no writer yet. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 318 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift, event replay DONE. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16b-owner-decision:v2","workItemId":"wi:mini-dsh:PB-ACTOR-ROLE-001","stableKey":"PB-ACTOR-ROLE-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PB-ACTOR-ROLE-001:AC-PB-ACTOR-ROLE-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

After the run, the domain's first rows recorded the ledger's own cast through the shipped seams: actors `actor:mini-dsh:owner` (HUMAN, the go-gate holder) and `actor:mini-dsh:agent:mini-real-use-lane` (AGENT, the lane that has claimed every item), roles `role:mini-dsh:owner` (GOVERNANCE — the referent the approvals domain's `required_role` already names) and `role:mini-dsh:executor` (EXECUTION), assigned as `asg:mini-dsh:144` and `asg:mini-dsh:145` — events 140–145. The persistent ledger migrated schema 4→5 in place on first open and now folds a 1/2/3/4/5 mixed timeline clean (97 + 19 + 2 + 14 + 13 stamps over 145 events). An idempotent rerun passed (`alreadyComplete: true`, zero tool calls, doctor 0, drift 0) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). The v1.6b plan's version-2 batch is now complete in both items — decisions, approvals, resources, and actor/roles all stand as ledger-recorded domains.

### 2026-09-20 — v1.6d entry: the decision domain's second gate use

The v1.6d prework from the v1.6b closing gate — the bilingual collaboration scope proposal (`docs/mini/v1.6d/fork-mini-DSH-v1.6d-collaboration-scope-proposal.md`) — ends with the §33 entry gate, and this entry is that gate running: the owner's go on the gate whose first option was the entry decision opened `dr:mini-dsh:v1.6d-entry` `BLOCKING` with three options (`enter-v1.6d-stage-a` recommended, `enter-v1.6d-one-batch`, `keep-accumulating`) and resolves it selecting `enter-v1.6d-stage-a` — decision `dc:dr:mini-dsh:v1.6d-entry:147`, events 146–147, the decisions domain's second gate use after `dr:mini-dsh:v1.6b-entry` (event 109). No lane run and no work item this increment: the recorded activity is the gate itself, written through the shipped `openDecisionRequest`/`recordDecision` seams. The persistent ledger now folds a 1/2/3/4/5 mixed timeline clean (97 + 19 + 2 + 14 + 15 stamps over 147 events); the replay audit reads 0 drift and the doctor reports 0 issues on the active v1.6b plan version. Next: the v1.6d plan yaml (stage A batch — work assignments, handoffs, per-actor claim visibility) is drafted with the first stage-A increment, and its items become claimable then.

### 2026-09-20 — PA-WORK-ASSIGNMENT-001: work assignments become duty rows

Stage A's first increment landed as the v1.6d plan's version-1 opener: the work-assignment domain (blueprint §18, adapted — the blueprint's actor/role foreign keys reference the v1.6b rows directly, and its six assignment kinds close uppercase), arriving as schema version 6 through one appended 5→6 migration. One required vocabulary event (`work/assigned`) bumps the event format to 6 under the adjacent-reads contract; `assignWorkItem` writes the assignment and its event atomically and refuses a second live `PRIMARY` on one item — the partial unique index, the seam, and the replay fold all enforce it; `readProjectWorkAssignments` lists the project newest-first with item, actor, and role labels resolved through joins; replay-audit and doctor parity cover the family in both sweeps; the read-only `/project assignments` view renders it. `ENDED` and the acceptance and completion timestamps stay reserved with no writer. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 330 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16d-collaboration:v1","workItemId":"wi:mini-dsh:PA-WORK-ASSIGNMENT-001","stableKey":"PA-WORK-ASSIGNMENT-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PA-WORK-ASSIGNMENT-001:AC-PA-WORK-ASSIGNMENT-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

After the run, the domain's first rows recorded the stage-A duty structure on the item the lane will claim next: `wa:mini-dsh:159` — the lane `PRIMARY` as `executor` — and `wa:mini-dsh:160` — `owner` `ACCOUNTABLE` — on `PA-HANDOFF-RECORD-001`, events 159–160, the timeline's first format-6 stamps. The persistent ledger migrated schema 5→6 in place on first open and now folds a 1/2/3/4/5/6 mixed timeline clean (97 + 19 + 2 + 14 + 15 + 13 stamps over 160 events). An idempotent rerun passed (`alreadyComplete: true`, zero tool calls, doctor 0, drift 0) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). `PA-HANDOFF-RECORD-001` (schema 6→7) and `PA-CLAIM-VISIBILITY-001` stay READY in version 1.

### 2026-09-20 — PA-HANDOFF-RECORD-001: handoffs record the pass between actors

Stage A's second increment landed the handoff domain (blueprint §28, adapted like the work-assignment domain — `project_id` derives through the work item row, the blueprint's `summary_content_id` indirection becomes inline summary text, artifact and memory references ride as JSON objects the seam serializes, and the blueprint's unvalued `handoff_kind` closes to `DELEGATE`/`RETURN`), arriving as schema version 7 through one appended 6→7 migration. One required vocabulary event (`handoff/recorded`) bumps the event format to 7 under the adjacent-reads contract; `recordHandoff` writes the handoff and its event atomically and requires exactly one recipient — an actor or a role — enforced by a recipient CHECK, the seam, and the payload codec alike; `readProjectHandoffs` lists the project newest-first with item, sender, and recipient labels resolved through joins; replay-audit and doctor parity cover the family in both sweeps; the read-only `/project handoffs` view renders it. `accepted_at_ms` stays reserved with no writer. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 342 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16d-collaboration:v1","workItemId":"wi:mini-dsh:PA-HANDOFF-RECORD-001","stableKey":"PA-HANDOFF-RECORD-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PA-HANDOFF-RECORD-001:AC-PA-HANDOFF-RECORD-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

After the run, the domain's first row recorded the pass the stage's closing item now waits on: `ho:mini-dsh:168` — `PA-CLAIM-VISIBILITY-001` handed `DELEGATE` from the lane agent to whoever holds the `executor` role, its artifact refs pinning the v1.6d plan — riding events 161–168, the timeline's first format-7 stamps. The persistent ledger migrated schema 6→7 in place on first open and now folds a 1/2/3/4/5/6/7 mixed timeline clean (97 + 19 + 2 + 14 + 15 + 13 + 8 stamps over 168 events). An idempotent rerun passed (`alreadyComplete: true`, zero tool calls, doctor 0, drift 0) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). `PA-CLAIM-VISIBILITY-001` — per-actor claim visibility and the two-agent evidence run — closes stage A.

### 2026-09-20 — PA-CLAIM-VISIBILITY-001: the agent todo view gains per-actor claim visibility

Stage A's closing item is the scheduling-projection change the scope proposal promised, with no schema or event-format change: the todo views gain per-actor claim visibility — a `viewerIdentity` on the todo-view request, carried through the resolved spec and echoed on the view. With a viewer named, an entry another identity holds a live claim on drops out while unclaimed and own entries stay; without one, the view stays the full queue the `/project todo --agent` command reads. `project_work_next` resolves the viewer at its boundary — a session that names an agent filters by `agent:<id>`, the same identity claims and evaluations record; a session-less execution has no identity to filter by and lists everything — and an empty viewer identity fails loud (`empty-viewer-identity`). The filter lives in the read: no new readiness blocker, no new authority.

The stage's evidence run then drove two registered AGENT actors through one item pass over the shipped seams: the second agent `actor:mini-dsh:agent:second` registered (event 169), the lane took `PRIMARY` as `executor` on this item (`wa:mini-dsh:170`), and the second agent claimed under `agent:second` (lease 171) — the filtered views split exactly as designed: the lane's view hid the item, the second agent's view showed it held, the unfiltered view showed it labeled — then the pass was recorded as `ho:mini-dsh:172` (`DELEGATE` to the lane) and the second agent released, returning the item to `READY` for the lane. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 345 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16d-collaboration:v1","workItemId":"wi:mini-dsh:PA-CLAIM-VISIBILITY-001","stableKey":"PA-CLAIM-VISIBILITY-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:PA-CLAIM-VISIBILITY-001:AC-PA-CLAIM-VISIBILITY-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

With the item DONE, stage A closes: the persistent ledger folds a 1/2/3/4/5/6/7 mixed timeline clean (97 + 19 + 2 + 14 + 15 + 13 + 20 stamps over 180 events), now holding two registered AGENT actors and one recorded pass between them. An idempotent rerun passed (`alreadyComplete: true`, zero tool calls, doctor 0, drift 0) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). All three v1.6d stage-A items are DONE in version 1; stage B — scope reservations and collaboration conflicts — awaits the owner's gate.

### 2026-09-20 — v1.6d stage B entry and SB-SCOPE-RESERVATION-001: scope reservations prevent overlap

Stage B opened the way stage A's increments did — the gate first, then the batch with its first domain. The owner's go on the stage-B offer became decision `dr:mini-dsh:v1.6d-stage-b` (`BLOCKING`, options `enter-v1.6d-stage-b` recommended / `keep-accumulating`) resolved selecting `enter-v1.6d-stage-b` — `dc:dr:mini-dsh:v1.6d-stage-b:182`, events 181–182, the decisions domain's third gate use after the v1.6b and v1.6d entries. The v1.6d plan then superseded its own version 1 (its three DONE stage-A items frozen) and activated version 2, the stage-B batch: scope reservations first, collaboration conflicts second.

The scope-reservation domain (blueprint §21, adapted — `project_id` stays because the scope's uniqueness is project-scoped, foreign keys reference the work-item and actor rows directly, the blueprint's `mode` column is dropped with every reservation shipping exclusive, and the unvalued `scope_kind` closes to `PATH`) arrived as schema version 8 through one appended 7→8 migration. Three required vocabulary events (`scope/reserved`, `scope/released`, `scope/expired`) bump the event format to 8; the lifecycle mirrors the work leases — `reserveScope` expires a stale row still holding the scope inside its own transaction, `releaseScopeReservation` frees it, `reapExpiredScopeReservations` moves the lapsed batch, and one active reservation per project scope is a partial unique index, a seam refusal, and a fold check at once; `readProjectScopeReservations` lists the project newest-first with item and actor labels resolved through joins; replay-audit and doctor parity cover the family in both sweeps; the read-only `/project reservations` view renders each row's lifecycle. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 362 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16d-collaboration:v2","workItemId":"wi:mini-dsh:SB-SCOPE-RESERVATION-001","stableKey":"SB-SCOPE-RESERVATION-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:SB-SCOPE-RESERVATION-001:AC-SB-SCOPE-RESERVATION-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

After the run, the domain's first rows partitioned the stage-B work the two agents will do next, both on `SB-CONFLICT-RECORD-001`: `sr:mini-dsh:194` — the lane reserving `packages/experimental/project-ledger` — and `sr:mini-dsh:195` — the second agent reserving `packages/experimental/mini-profile` — with a third reservation on the lane's scope refused (`duplicate-reservation` naming the holding row), the prevention working against the very cast that will test the conflicts domain. The persistent ledger now folds a 1/2/3/4/5/6/7/8 mixed timeline clean (97 + 19 + 2 + 14 + 15 + 13 + 22 + 13 stamps over 195 events), migrated schema 7→8 in place on first open. An idempotent rerun passed (`alreadyComplete: true`, zero tool calls, doctor 0, drift 0) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). `SB-CONFLICT-RECORD-001` (schema 8→9, format 9) stays READY in version 2.

### 2026-09-20 — SB-CONFLICT-RECORD-001: collaboration conflicts record overlaps the reservations slipped

Stage B's closing item is the conflict domain (blueprint §29, adapted — `project_id` derives through the first work item row like the assignment and handoff tables, the blueprint's `description_content_id` indirection becomes inline description text, its nullable raiser and work-item columns close to required, and its `conflict_kind` closes to `SCOPE_OVERLAP`: the containment overlap the equal-value uniqueness check does not judge) as schema version 9 through one appended 8→9 migration. Two required vocabulary events (`conflict/recorded`, `conflict/resolved`) bump the event format to 9; `recordConflict` writes one recorded overlap between two distinct work items of one project raised by a registered actor, `resolveConflict` closes an open conflict through a recorded v1.6b decision of the same project — the decision the ledger recorded as the answer, checked by joining through `decision_requests` because the `decisions` table carries no project column — and a resolved conflict never resolves again; `readProjectConflicts` lists the project newest-first with both items, the raiser, and the resolution resolved through joins; replay-audit and doctor parity cover the family in both sweeps; the read-only `/project conflicts` view renders each row's lifecycle. Claim by `agent:mini-real-use-lane`, stored verifier `pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile` (exit 0; 376 tests, per-file 100% coverage across all three packages), item DONE, doctor 0 issues, replay audit 0 drift. First-run report line:

```json
{"lane":"real-use","ledger":"~/.dsh/project-ledger/ledger.sqlite","alreadyComplete":false,"planVersionId":"plv:mini-dsh-v16d-collaboration:v2","workItemId":"wi:mini-dsh:SB-CONFLICT-RECORD-001","stableKey":"SB-CONFLICT-RECORD-001","verifierResults":[{"criterionId":"ac:wi:mini-dsh:SB-CONFLICT-RECORD-001:AC-SB-CONFLICT-RECORD-001","command":"pnpm exec vitest run project-ledger project-ledger-sqlite mini-profile","exitCode":0}],"itemStatus":"DONE","doctorIssues":0,"replayDrift":0,"replayItemStatus":"DONE","toolCalls":{"project_work_next":1,"project_work_claim":1,"project_work_update":1}}
```

After the run, the domain's first row recorded the stage-B near-miss as the fact it now has a home for: `cf:mini-dsh:203` — `SCOPE_OVERLAP` between `SB-SCOPE-RESERVATION-001` and this item, raised by the second agent, OPEN — whose description states exactly what happened: the two stage-B reservations (`sr:mini-dsh:194`, `sr:mini-dsh:195`) held, a third attempt at the held scope was refused as duplicate, and a nested path under either reservation would have slipped the equal-value check. The plan document itself is unchanged, so the run's import is idempotent and version 2 stays the active version. The persistent ledger migrated schema 8→9 in place on first open and now folds a 1/2/3/4/5/6/7/8/9 mixed timeline clean (97 + 19 + 2 + 14 + 15 + 13 + 22 + 13 + 8 stamps over 203 events). An idempotent rerun passed (`alreadyComplete: true`, zero tool calls, doctor 0, drift 0) and the 4K slice rerun stayed green (6 requests, max 2,444/4,096 estimated tokens, DONE). All five v1.6d items — three stage-A, two stage-B — are DONE in version 2; stage B closes and the v1.6d completion report can follow as its own gated step.

### 2026-09-21 — v1.7 stage A: the activation backfill and the benchmark lifecycle migration

The owner's go on the [v1.7 improvement plan](v1.7/fork-mini-DSH-v1.7-improvement-plan.md) entered stage A, and its first increment shipped the activation writer itself: `activatePlanVersion` moves a `DRAFT` version to `ACTIVE` with its `activated_at_ms` stamp, the `plans.current_version_id` pointer, and the strict `plan/version-activated` event in one `BEGIN IMMEDIATE`; the fold owns the version lifecycle and the replay audit owns lifecycle parity, with the doctor converting replay drift instead of re-implementing it — event format 10, schema 9 (the columns existed since v1). This increment closes the activation hole in the recorded history: the persistent ledger's three currently-ACTIVE versions (`plv:mini-dsh-post-v16a-increments:v5`, `plv:mini-dsh-v16b-owner-decision:v2`, `plv:mini-dsh-v16d-collaboration:v2`) received their activation events at sequences 204–206 (format 10, actor `owner-backfill`, stamps recorded retroactively at backfill time because the raw era never wrote them), and the pointer ownership the raw era never exercised landed with them — all three plans now name their ACTIVE version. The six superseded versions keep their event-timeline shape: in an append-only timeline a retroactive activation would land after its recorded supersede and the fold rightly refuses that sequence. Replay audit reads 0 drift over the 206-event, ten-stamp timeline; the doctor reports 0 issues on the active v1.6d version. The benchmark workers moved off direct lifecycle SQL in the same increment — the context-light worker's unconditional `UPDATE plan_versions SET status = 'ACTIVE'` (no `WHERE` clause: it activated every version in its database) and the real-use worker's pointer-less raw activation both go through the writer now — and the deterministic 4K rerun stayed green keyless (6 requests, max 2,444/4,096 estimated tokens, verifier exit 0, DONE). No lane run and no work item this increment: the recorded activity is owner bookkeeping and the code increment; keyed lane claims of the v1.7 plan's items and the live-4K rerun wait for a keyed environment. Next: F-01's trusted verifier consumer.

## Status toward the gate



Items completed through the ledger: 35 (15 golden-plan items by the v1.6a build itself, plus twenty through the lane entries above — entries 17 and 20, the v1.6d entry and stage-B entry decisions, complete no item). BOOT/4K regression: none recorded — the 4K bar is itself a completed ledger item (`PW-4K-GATE-001`, stored verifier `run-4k.sh`, exit 0) and every increment's rerun stays green; the BOOT acceptance suites stay green. Owner confirmation of value: **given 2026-09-19** through the established go-gate and recorded as decision `dr:mini-dsh:v1.6b-entry` (`enter-v1.6b` selected) — v1.6b is entered and closed with its completion report; **given 2026-09-20 for v1.6d** as decision `dr:mini-dsh:v1.6d-entry` (`enter-v1.6d-stage-a` selected) — stage A is entered and closed; **given 2026-09-20 for stage B** as decision `dr:mini-dsh:v1.6d-stage-b` (`enter-v1.6d-stage-b` selected) — stage B is entered and closed, and all five v1.6d items are DONE.
